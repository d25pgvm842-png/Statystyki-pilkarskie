import { RouteLoading } from "@/components/layout/route-loading";

export default function DashboardLoading() {
  return <RouteLoading label="Ładowanie widoku" metricCount={4} contentHeight="h-72" />;
}
